library("qtl")

#sample data, for making sure the thing does what you want
data("mapthis")

#NB: You will need to use the command "read.cross(data)", making sure to list the genotypes and alleles (0,1,2 I think)

